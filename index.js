function divide(num1, num2) {
    num1 = 2000
    num2 = 100
    return num1 / num2
}
divide()

const square = (parameter) => parameter * parameter
square(2)

const add = (par1, par2) => par1 + par2
add(2, 3)